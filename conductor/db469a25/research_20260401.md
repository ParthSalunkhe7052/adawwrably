# Research: Phase 1 Foundation - Project Setup & Design System

**Date**: 2026-04-01

## 1. Executive Summary
The project is currently a fresh Vite + React + TypeScript installation. It lacks all necessary dependencies for Phase 1 as outlined in `PLAN-1-FOUNDATION.md`. Tailwind CSS v4 and shadcn/ui are not installed. `index.css` and `index.html` are still at their template defaults.

## 2. Technical Context
- **Current package.json**: `react@18.3.1`, `react-dom@18.3.1`. Lacks `tailwindcss`, `@tailwindcss/vite`, and `shadcn` related packages. [package.json:13]
- **Current vite.config.ts**: Minimal setup with only `react()` plugin. No `tailwindcss` plugin. [vite.config.ts:6]
- **Current index.css**: Default Vite boilerplate. No Tailwind directives or design tokens. [src/index.css:1]
- **Current index.html**: Default title "Vite + React + TS", no dark mode class, no favicon. [index.html:1]

## 3. Findings & Analysis
- **Missing Tailwind v4**: Not installed or configured.
- **Missing shadcn/ui**: Not initialized.
- **Missing Assets**: `public/products/` directory does not exist. `Logo.png` and `image copy.png` are in the root but not yet moved to `public/`.
- **Inconsistent React Version**: Plan mentions React 19, currently at 18.3.1. I'll need to upgrade if strictly required by shadcn/ui.

## 4. Technical Constraints
- **Tailwind v4 Integration**: Requires `@tailwindcss/vite` plugin and specific `@theme` directives in CSS.
- **shadcn/ui Initialization**: Requires `npx shadcn@latest init` which will modify `tsconfig.json` and create `components.json`.

## 5. Architecture Documentation
- Standard Vite + React template structure.
- `.gitignore` exists and is standard.

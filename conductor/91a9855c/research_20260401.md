# Research: Phase 3 - Assembly & Polish

## Current State
- All components built in Phase 2.
- `App.tsx` exists but was a placeholder.
- `src/index.css` was fragmented between Tailwind v4 imports and custom tokens.
- `vite.config.ts` and `tsconfig.json` are configured.

## Findings
- `App.tsx` needs to import and assemble all sections in order.
- `src/index.css` needs to consolidate all custom animations (marquee, spin-slow, scroll-bounce) and design tokens.
- IntersectionObserver in `SideHUD` needs specific section IDs to work.
- Build failed initially due to missing `formatPrice` utility and incorrect `lucide-react` icon names.

## Pointers
- `src/App.tsx`
- `src/index.css`
- `src/lib/utils.ts`
- `src/components/layout/SideHUD.tsx`
